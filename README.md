# User Directory

This project fetches user data from JSONPlaceholder API and enhances it with avatar images from the DiceBear API. It displays user information in a clean, responsive interface with server-side rendering.

## Features
- Server-side rendering for optimal performance
- Responsive grid layout for various screen sizes
- Custom avatar generation for each user
- Error handling and fallback UI